spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/seniordb  # или удалённый хост
    username: postgres
    password: ideappad147
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update  # или none
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
#        default_schema: project

server:
  port: 8080
jwt:
  secret: asncnJNAJKbjkd518d48eSafsef5851dfrg473mslasdef785146efdg
  lifetime: 30m

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG